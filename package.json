{
  "name": "@statespace/monorepo",
  "version": "0.1.0-beta.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "examples",
    "packages/*",
    "packages/studies/*",
    "tools/*"
  ],
  "scripts": {
    "context": "bun run --filter @statespace/scripts context",
    "example:tower-of-hanoi": "bun run --filter @statespace/examples tower-of-hanoi"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {},
  "trustedDependencies": [
    "kuzu"
  ],
  "dependencies": {
    "@datastructures-js/priority-queue": "^6.3.4"
  }
}
