fragment CreateInfo on CreateInfo {
  nodesCreated
  relationshipsCreated
}

fragment UpdateInfo on UpdateInfo {
  nodesCreated
  nodesDeleted
  relationshipsCreated
  relationshipsDeleted
}

fragment UpsertInfo on UpsertInfo {
  nodesCreated
  nodesUpdated
}

fragment StateInfo on State {
  id
  hash
  codex
  api_version
  timestamp_created
  timestamp_updated
}

fragment TransitionInfo on Transition {
  id
  rule_name
  cost
  metadata
  api_version
  timestamp_created
  timestamp_updated
}
